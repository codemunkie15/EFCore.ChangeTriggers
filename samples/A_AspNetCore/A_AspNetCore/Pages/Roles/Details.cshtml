@page
@model A_AspNetCore.Pages.Roles.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Role</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role.Enabled)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role.Enabled)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Role.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>

<h3>History</h3>

<table class="table">
    <thead>
        <tr>
            <th>Description</th>
            <th>Old Value</th>
            <th>New Value</th>
            <th>Changed at</th>
            <th>Changed by</th>
            <th>Change source</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.RoleChangeEvents)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OldValue)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NewValue)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ChangedAt)
                </td>
                <td>
                    @if (item.ChangedBy != null)
                    {
                        @Html.DisplayFor(modelItem => item.ChangedBy.Name)
                    }
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ChangeSource)
                </td>
            </tr>
        }
    </tbody>
</table>